root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o tmp/worker.exe ./cmd/worker/main.go"
  bin = "tmp/worker"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "node_modules"]
  delay = 1000

[log]
  time = true
